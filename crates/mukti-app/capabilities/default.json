{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the one window is allowed to ask the Rust side to do. Without a file like this in this exact folder, Tauri grants nothing: the generated permission set comes out as an empty {} and every plugin call is refused at runtime. That is what made Open, Save as... and drag-and-drop fail in 0.4.0, separately from the window's script not starting at all.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
