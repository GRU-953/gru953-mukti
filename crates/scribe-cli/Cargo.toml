[package]
name = "scribe-cli"
description = "GRU953 Scribe on the command line: convert legacy Bangla files to Unicode."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[[bin]]
name = "scribe"
path = "src/main.rs"

[dependencies]
gru953-scribe = { path = "../scribe-core", version = "0.3.0" }
scribe-formats = { path = "../scribe-formats", version = "0.3.0" }

[lints]
workspace = true
